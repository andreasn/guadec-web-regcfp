// This code is used both on the client and in the server
//  this means we get to reuse the same functions, and show the same prices
(function(exports) {
  exports.calculate_price = function(registration) {
  }
})(typeof exports === 'undefined' ? this['registration']{} : exports);
